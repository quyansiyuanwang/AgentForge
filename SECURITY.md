# 安全策略

## 报告漏洞

请勿在公开 issue 或讨论区披露可利用细节。优先使用 GitHub Security Advisories 的私密报告入口；若仓库未启用该入口，请联系项目维护者并提供最小复现、影响范围和修复建议。

## 设计边界

远程内容先在隔离暂存区执行路径穿越、绝对路径、符号链接逃逸、特殊文件、大小限制和 SHA-256 校验。普通 sync 严格使用 lock/vendor；只有显式 `skill update` 或 `mcp update` 才刷新依赖。脚本、MCP command、网络权限和凭据要求在 preview/doctor 中展示，安装含脚本内容需要交互确认或显式 CI flag。工具只记录环境变量名称或占位符，不读取 secret 值。

## 支持版本

安全修复优先覆盖最新 v0.1.x；旧版本请升级到最新补丁版本。第三方来源的安全性和许可证由使用者审核，AgentForge 不保证外部内容本身无恶意。
